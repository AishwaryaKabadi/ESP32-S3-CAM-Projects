// ESP32-S3 CAM - Blink Test

#define LED_PIN 2   // Change this if your board uses a different LED pin

void setup() {
  pinMode(LED_PIN, OUTPUT);
}

void loop() {
  digitalWrite(LED_PIN, HIGH);
  delay(500);

  digitalWrite(LED_PIN, LOW);
  delay(500);
}
